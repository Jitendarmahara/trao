/**
 * Backend scaffold. The Express API (Step 10) and the batch CLI (Step 9) live here
 * and call into @interview-prep-kit/core for all pipeline logic.
 */
function main(): void {
  console.log('Interview Prep Kit backend — scaffold. Real server arrives in Step 10.');
}

main();
